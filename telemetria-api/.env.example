# Plantilla para docker-compose.yaml. Copiar a ".env" (ese archivo SI esta en .gitignore) y
# rellenar con el mismo valor que uses como DB_PASSWORD en application-local.properties.
DB_PASSWORD=cambia-esto
